import Window from "../common/Window";

const ChatDmWindow = ({ className }: { className?: string }) => {
  return <Window title="Dm Room | 칭구이름 " className={className}></Window>;
};

export default ChatDmWindow;
